# Local environment configuration
ENVIRONMENT=local

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres-local:5432/mafia_local
REDIS_URL=redis://redis-local:6379/0
RABBITMQ_URL=amqp://guest:guest@rabbitmq-local:5672

# TON (Mock for local)
TON_NETWORK=sandbox
TON_ENDPOINT=http://ton-local:8081
TON_USE_SANDBOX=true
MOCK_BLOCKCHAIN_CALLS=true

# Security (test keys for local only)
JWT_SECRET=local_secret_key_for_development_only
JWT_ALG=HS256
WALLET_ENCRYPTION_KEY=local_encryption_key_32_bytes_ok!

# Telegram (test values)
TELEGRAM_BOT_TOKEN=test:token:for:local:development
TELEGRAM_WEBAPP_URL=http://localhost:3000

# Services
MEDIASOUP_URL=http://localhost:4443

# Features
AUTO_CREATE_WALLET=true

# CORS
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8001"]